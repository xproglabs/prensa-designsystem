import { ArgsTable, Meta } from '@storybook/addon-docs/blocks';
import {CodeBlock, Space} from 'prensa-docs';
import {ThemeProvider} from 'styled-components';
import {theme} from '../../styles/theme';
import Typography from './index'

<Meta title="Components/Typography" />

<ThemeProvider theme={theme}>
  <Typography textAlign='center' color="primary1" element='cite' ml={5} mr={2} fontSize="150px" lineHeight="100%" transform='uppercase' md={{fontSize: '100px', transform: 'lowercase', color: 'success1'}}>The quick brown fox</Typography>
</ThemeProvider>