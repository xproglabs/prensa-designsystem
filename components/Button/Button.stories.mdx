import { ArgsTable, Meta, Story } from '@storybook/addon-docs/blocks';
import Button from './index';

export const Template = (args) => <Button {...args} />;

<Meta title="Components/Button" component={Button} />

# Checkbox

With `MDX` we can define a story for `Checkbox` right in the middle of our Markdown documentation.

<p>testando</p>

<div style={{backgroundColor: 'red'}}>
  qualquer coisa
</div>

> Testando
## Testando 2
### Testando 3


## Testando 2
### Testando 3

<Story name="Primary" args={{children: 'Avançar', variant: 'filled'}} >
  {Template.bind({})}
</Story>

<br/>
<br/>
<br/>

# Tabela de props
<ArgsTable of={Button} />

`Testando uma prop`
`$button-primary`