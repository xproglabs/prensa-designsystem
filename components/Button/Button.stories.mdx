import { ArgsTable, Meta } from '@storybook/addon-docs/blocks';
import Button from './index';
import {CodeBlock} from 'prensa-docs';
import DownloadIcon from '../../icons/ArrowCircleDownBlack18Dp';
import ArrowRightIcon from '../../icons/IcArrowForward';

export const Template = (args) => <Button {...args} />;

export const Space = props => {
  const unit = 8;
  const {size} = props;
  return <div style={{marginTop: size * unit}} />
};

<Meta title="Components/Button" component={Button} />

# Button

<Space size={4} />

<pre>
  <CodeBlock>{`import {Button} from 'prensa'`}</CodeBlock>
</pre>

<Space size={6} />

### Variações
O Button carrega duas variações de estilo, **Filled** e **Outlined**. Elas alteram a escolha da propriedade css para a estrutura do botão. Para filled, usamos background-color. Já outlined, usa border-color com backgorund transparente.

<Button variant="filled" style={{marginRight: 8}}>Avançar</Button>
<Button variant="outlined" style={{marginRight: 8}}>Avançar</Button>

<Space size={2} />

<pre>
  <CodeBlock children={`
  <Button variant="filled">Avançar</Button>
  <Button variant="outlined">Avançar</Button>
  `}/>
</pre>


<Space size={6} />

### Esquema de cores
O componente possui acesso a toda a família de cores do design system. Usamos a prop `color` para a escolha, passando o nome da cor em forma de string, sem o prefixo color: `color="primary-1"`, `color="editorial-2"`.

<Button variant="filled" color='primary-1' style={{marginRight: 8}}>Lorem</Button>
<Button variant="filled" color='editorial-2' style={{marginRight: 8}}>Ipsum</Button>

<Space size={2} />

<pre>
  <CodeBlock children={`
  <Button variant="filled" color="primary-1">Avançar</Button>
  <Button variant="outlined" color="editorial-2">Avançar</Button>
  `}/>
</pre>

<Space size={6} />

### Border radius
O componente possui acesso a família de variações para o css border-radius. Podemos alternar entre um botão quadrado e dois níveis de radius. A props é `radius` e as opções são `false`, `default` e `alternative`.

<Button variant="filled" color='primary-1' radius={false} style={{marginRight: 8}}>Lorem</Button>
<Button variant="filled" color='primary-1' radius='default' style={{marginRight: 8}}>Lorem</Button>
<Button variant="filled" color='primary-1' radius='alternative' style={{marginRight: 8}}>Lorem</Button>

<Space size={2} />

<pre>
  <CodeBlock children={`
  <Button variant="filled" color="primary-1" radius={false}>Lorem</Button>
  <Button variant="outlined" color="primary-1" radius='default'>Lorem</Button>
  <Button variant="outlined" color="primary-1" radius='alternative'>Lorem</Button>
  `}/>
</pre>

<Space size={6} />

### Tamanhos
O componente possui cinco tamanhos de altura. Para alterá-los basta usar a prop `size`.

`size={1} | 32px`
`size={2} | 40px`
`size={3} | 48px`
`size={4} | 56px`
`size={5} | 64px`

<Button variant="filled" color='primary-1' size={1} style={{marginRight: 8}}>Entrar</Button>
<Button variant="filled" color='primary-1' size={2} style={{marginRight: 8}}>Entrar</Button>
<Button variant="filled" color='primary-1' size={3} style={{marginRight: 8}}>Entrar</Button>
<Button variant="filled" color='primary-1' size={4} style={{marginRight: 8}}>Entrar</Button>
<Button variant="filled" color='primary-1' size={5} style={{marginRight: 8}}>Entrar</Button>

<Space size={2} />

<pre>
  <CodeBlock children={`
  <Button variant="filled" color='primary-1' size={1}>Entrar</Button>
  <Button variant="filled" color='primary-1' size={2}>Entrar</Button>
  <Button variant="filled" color='primary-1' size={3}>Entrar</Button>
  <Button variant="filled" color='primary-1' size={4}>Entrar</Button>
  <Button variant="filled" color='primary-1' size={5}>Entrar</Button>
  `}/>
</pre>

<Space size={6} />

### Estados
O componente possui dois estados: `disabled` e `hover`. Para acionar o disabled, deve-se utilizar a props `disabled={true}`. Já o hover aciona automaticamente ao passar o mouse sobre o botão. O estilo de ambos é sempre fixo.

<Button variant="filled" color='primary-1' disabled={true} style={{marginRight: 8}}>Disabled</Button>
<Button variant="outlined" color='primary-1' disabled={true} style={{marginRight: 8}}>Disabled</Button>

<Space size={2} />

<pre>
  <CodeBlock children={`
  <Button variant="filled" color='primary-1' disabled={true}>Disabled</Button>
  <Button variant="outlined" color='primary-1' disabled={true}>Disabled</Button>
  `}/>
</pre>

<Space size={6} />

### Funcionalidades
O componente pode receber uma prop chamada `onClick` que permite a passagem da ação do clique realizado pelo usuário. Também pode receber a prop `fullWidth` que faz com que o componente assuma o formato 100% para o estilo.

<Space size={2} />

<pre>
  <CodeBlock children={`
  import {Button} from 'prensa';

  const handleClick = () => {
    console.log('click')
  }

  const Component = props => {
    return <Button variant="filled" color="secondary" onClick={handleClick()}>Avançar</Button>;
  }
  `}/>
</pre>

<Space size={10} />

### Botão com ícone
O componente pode receber ícones do lado esquerdo ou direito através das props `leftIcon` e `rightIcon`, respectivamente. Para utilizá-las basta importar um ícone SVG como componente e passar o mesmo na props.

<div style={{display: 'flex'}}>
  <Button variant="filled" color='primary-1' leftIcon={<DownloadIcon />} style={{marginRight: 8}}>Com ícone</Button>
  <Button variant="filled" color='primary-1' rightIcon={<ArrowRightIcon />} style={{marginRight: 8}}>Com ícone</Button>
  <Button fullWidth variant="filled" color='primary-1' rightIcon={<ArrowRightIcon />}>Com ícone</Button>
</div>

<Space size={2} />

<pre>
  <CodeBlock children={`
  import {Button} from 'prensa';
  import {ArrowCircleDownBlack18Dp as DownloadIcon, IcArrowForward as ArrowRightIcon} from 'prensa/icons';

  <Button variant="filled" color='primary-1' leftIcon={<DownloadIcon />}>Com ícone</Button>
  <Button variant="filled" color='primary-1' rightIcon={<ArrowRightIcon />}>Com ícone</Button>
  <Button fullWidth variant="filled" color='primary-1' rightIcon={<ArrowRightIcon />}>Com ícone</Button>
  `}/>
</pre>

<Space size={10} />

# API
<ArgsTable of={Button} />