import {ArgsTable, Meta} from '@storybook/addon-docs/blocks';
import {ThemeProvider} from 'styled-components';
import {Space, CodeBlock} from 'prensa-docs';
import {theme} from 'storybook/theme';

import Block from './index';

<Meta title="Block / Custom" />

# Block / Custom

<Space size={3} />

Block é um componente baseado em uma `<div>` para entregar posicionamentos blocados na tela.

<Space size={6} />

### Propriedade para estilos customizados

<Space size={3} />

Utilize a propriedade `custom` para escrever qualquer regra CSS.

<Space size={6} />

### Exemplo 1

`bgColor: primary2`

`*color: rgba(255, 200, 255, 1);`

`*padding: 10px`

<pre>
  <CodeBlock>{`<Block bgColor='primary2' custom='
    color: rgba(255, 200, 255, 1);
    padding: 10px;
  '></Block>`}</CodeBlock>
</pre>

<Space size={3} />

<ThemeProvider theme={theme}>
  <Block 
    bgColor='primary2'
    custom={`
      color: rgba(255, 200, 255, 1);
      padding: 10px;
    `}>
    <span>item1</span>
    <span>item2</span>
    <span>item3</span>
  </Block>
</ThemeProvider>

<Space size={6} />

### Exemplo 2

`row center`

`bgColor: primary1`

`*color: rgba(255, 200, 155, 3);`

`*padding: 20px`

<pre>
  <CodeBlock>{`<Block align= 'row' alignx='center' bgColor='primary1' custom='
    color: rgba(255, 200, 155, 3);
    padding: 20px;
  '></Block>`}</CodeBlock>
</pre>

<Space size={3} />

<ThemeProvider theme={theme}>
  <Block 
    bgColor='primary1'
    align='row'
    alignx='center'
    custom={`
      color: rgba(255, 200, 155, 3);
      padding: 20px;
    `}>
    <span>item1</span>
    <span>item2</span>
    <span>item3</span>
  </Block>
</ThemeProvider>

<Space size={6} />

### Exemplo 3

<Space size={6} />

### Exemplo 4

<Space size={6} />

### Exemplo 5
