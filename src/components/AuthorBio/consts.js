const AUTHOR_BIO = {
  element: 'span',
  mb: 2,
  textAlign: 'center',
  width: '100%',
  lg: {
    textAlign: 'left'
  }
}

const AUTHOR_TITLE = {
  element: 'span',
  mb: '4px',
  textAlign: 'center',
  width: '100%',
  lg: {
    textAlign: 'left'
  }
}

const CONTAINER = {
  aligny: 'middle',
  alignx: 'center',
  mx: 2,
  width: 'calc(100% - 32px)'
}

const CONTAINER_CENTER = {
  alignx: 'center',
  width: '100%',
  lg: {
    width: 'fit-content'
  }
}

const CONTAINER_IMAGE = {
  alignx: 'center',
  width: '100%',
  lg: {
    width: 'unset'
  }
}

const CONTAINER_MEDIAS = {
  align: 'row'
}

const CONTAINER_TITLES = {
  align: 'column',
  width: '100%'
}

const COLUMN_TITLE = {
  element: 'span',
  textAlign: 'center',
  width: '100%',
  lg: {
    textAlign: 'left'
  }
}

const CONTENT = {
  align: 'column',
  aligny: 'middle',
  px: 2,
  py: 2,
  width: 'calc(100% - 32px)',
  lg: {
    align: 'row'
  }
}

const CONTENT_IMAGE = {
  height: '80px',
  mb: 2,
  mr: '0px',
  width: '80px',
  lg: {
    height: '104px',
    mr: 2,
    width: '104px'
  }
}

const IMAGE = {
  bgColor: 'neutral2',
  height: '48px',
  minWidth: '48px',
  mr: 2,
  width: '48px'
}

export {
  AUTHOR_BIO,
  AUTHOR_TITLE,
  CONTAINER,
  CONTAINER_CENTER,
  CONTAINER_IMAGE,
  CONTAINER_MEDIAS,
  CONTENT,
  CONTENT_IMAGE,
  COLUMN_TITLE,
  IMAGE,
  CONTAINER_TITLES
}
