import { RadiusTokens } from '@prensa_tokens'

export const BORDER_DEFAULT: { b: string; borderColor: string; borderStyle: string; radius: RadiusTokens; } = {
  b: '1px',
  borderColor: 'neutral9',
  borderStyle: 'solid',
  radius: 'alternative'
}