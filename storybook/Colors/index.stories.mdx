import { Meta, Story, Canvas } from '@storybook/addon-docs';
import tokens from '../../tokens/default-tokens.json';
import Block from '../../components/Block'

import { theme, styled } from '../../stitches.config';

<Meta title="Components/Colors" />

export const ColorBlock = styled('div');

export const textStyle = {
  backgroundColor:'#fff',
  borderRadius: '8px',
  margin: '30px 12px',
  textAlign: 'center',
  padding: '4px',
  fontSize: '13px'
};

export const AllColors = () => (Object.keys(tokens.colors)).map(color => (
  <>
    <ColorBlock css={{$$size: '110px', display:'inline-block',width:'$$size', height: '$$size',backgroundColor: `$colors$${color}`}}>
      <ColorBlock css={{...textStyle}}>
      {color}
      <br/>
      {tokens.colors[color]}
      </ColorBlock>
    </ColorBlock>
    {color.indexOf('1000') !== -1 ? <ColorBlock css={{height:'1rem', width: '100%'}}></ColorBlock> : null}
  </>
));

## All Colors

<Story name="All Colors">
  <AllColors />
</Story>
