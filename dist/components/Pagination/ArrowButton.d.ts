/// <reference types="react" />
import { ArrowButtonProps } from './types';
declare const ArrowButton: {
    ({ borderColor, color, direction, height, radius, width }: ArrowButtonProps): JSX.Element;
    defaultProps: {
        borderColor: string;
        color: string;
        height: string;
        radius: string;
        width: string;
    };
};
export { ArrowButton };
