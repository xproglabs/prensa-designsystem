/// <reference types="react" />
import { AuthorBioProps } from './types';
declare const AuthorBio: ({ amp, bio, bioProps, containerProps, href, imagePath, imageProps, name, nameProps, title, titleProps, facebookUrl, instagramUrl, linkedinUrl, twitterUrl }: AuthorBioProps) => JSX.Element;
export { AuthorBio };
