/// <reference types="react" />
import { AuthorProfileProps } from './types';
declare const AuthorProfile: ({ amp, authorNameProps, containerProps, date, dateTimeProps, href, image, imageWrapProps, title, titleProps, name, newsTitle, newsTitleHref, newsTitleProps }: AuthorProfileProps) => JSX.Element;
export default AuthorProfile;
