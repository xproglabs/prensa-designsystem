/// <reference types="react" />
import { AuthorProfileProps } from './types';
declare const TeaserProfile: ({ amp, authorNameProps, className, containerProps, domain, href, image, imageWrapProps, subtitleContainer, title, titleProps, name, }: AuthorProfileProps) => JSX.Element;
export default TeaserProfile;
