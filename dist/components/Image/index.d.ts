/// <reference types="react" />
import * as T from './types';
declare const Image: {
    ({ amp, custom_class, layout_desktop, layout_mobile, title }: T.ImageProps): JSX.Element;
    defaultProps: {
        amp: boolean;
    };
};
export default Image;
