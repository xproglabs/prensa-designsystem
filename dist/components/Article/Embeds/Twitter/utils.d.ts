/**
 * Prensa | getTweetId
 * @param url Expects an https tweet url
 * @example
 * https://twitter.com/${user}/status/${tweetId}
 * https://twitter.com/${user}/status/${tweetId}?ref_src=${...}
 * @returns tweetId
 */
export declare function getTweetId(url: string): string;
