/// <reference types="react" />
import { SideMenuAmpProps } from '../types';
declare const AmpMenu: ({ backgroundColor, children, height, id, layout, menuAnchor, px, py, width }: SideMenuAmpProps) => JSX.Element;
export default AmpMenu;
