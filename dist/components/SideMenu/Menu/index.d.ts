/// <reference types="react" />
import { SideMenuWebProps } from '../types';
declare const Menu: ({ backgroundColor, children, close, height, menuAnchor, open, px, py, width, }: SideMenuWebProps) => JSX.Element;
export default Menu;
